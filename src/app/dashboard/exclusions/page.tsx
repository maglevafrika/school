import PlaceholderPage from "../placeholder-page";
import { CircleSlash } from "lucide-react";

export default function ExclusionsPage() {
  return <PlaceholderPage title="Exclusion Rules" icon={CircleSlash} />;
}
