import PlaceholderPage from "../placeholder-page";
import { Calendar } from "lucide-react";

export default function SemestersPage() {
  return <PlaceholderPage title="Semester Management" icon={Calendar} />;
}
